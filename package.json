{
  "name": "agnostic",
  "version": "2.0.0",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "typecheck": "gleam build --target javascript && tsc -p tsconfig.json && tsc -p tsconfig.ffi.json && tsc -p tsconfig.ffi-js.json"
  },
  "devDependencies": {
    "@opentui/core": "0.4.5",
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
